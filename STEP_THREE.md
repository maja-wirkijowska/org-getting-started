# Using the .gitignore file

This repository includes a .gitignore (pronounced dot-git-ignore or simply git-ignore) file to be included in every repository to ensure that unwanted 
files (including .properties, .jar, target/, etc.) are ignored when adding or pulling code.
